#ifndef __UTILS__
#define __UTILS__

#include <iostream>
#include <pthread.h>
#include <cstring>
#include <cerrno>

/*
void error(const char *msg)
{
    std::cerr << msg << ": " << std::strerror(errno) << std::endl;
    pthread_exit(NULL);
}
*/
#endif